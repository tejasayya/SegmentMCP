{
  "mcpServers": {
    "segmentation-agent": {
      "command": "python",
      "args": ["main.py"],
      "cwd": "/absolute/path/to/your/SegmentMCP",
      "env": {
        "OPENAI_API_KEY": "your-openai-api-key-here",
        "KAGGLE_USERNAME": "your-kaggle-username",
        "KAGGLE_KEY": "your-kaggle-key"
      }
    }
  }
}

// Instructions:
// 1. Replace "/absolute/path/to/your/SegmentMCP" with your actual project path
// 2. Or run: python -c "from config import Config; Config.setup_project_structure()"
// 3. This will generate "claude_mcp_config_generated.json" with correct paths